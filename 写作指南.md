# 博客写作与发布指南

> 仓库：`C:\Users\34118\Desktop\blogs`（Hugo + Blowfish → Cloudflare Pages）
> 线上地址：https://euinblog.pages.dev/ ｜ GitHub：euinharry/my-blog，分支 `main`
> 本文所有命令都在仓库根目录 `C:\Users\34118\Desktop\blogs` 下执行。

---

## 一、速查：发一篇新文章

1. 建文件夹（英文小写，用 `-` 连接）：`content/posts/my-new-post/index.md`
2. 开头写 front matter（模板见第三节）
3. 图片丢到同一个文件夹里（见第四节）
4. 本地预览：`hugo server -D` → 打开 http://127.0.0.1:1313/
5. 发布：`git add -A` → `git commit -m "新增：xxx"` → `git push`

推到 `main` 后 Cloudflare Pages 自动构建部署，**约 1 分钟**后 https://euinblog.pages.dev/ 就是新的。

---

## 二、目录说明（哪些能动、哪些别动）

| 目录 | 作用 | 能不能动 |
| --- | --- | --- |
| `content/` | 你的文章、关于页、首页文案 | ✅ 主要在这写 |
| `content/posts/` | 博客文章 | ✅ |
| `config/_default/` | 站点配置（站名、菜单、外观） | ✅ 见第九节 |
| `assets/` | 需要被 Hugo 处理的图片和 CSS | ✅ 头像、背景图在这 |
| `static/` | 原样复制到网站根目录，不做处理 | ⚠️ 一般不用放东西 |
| `layouts/` | 覆盖主题的模板（现在只有归档页） | ✅ 谨慎 |
| `themes/blowfish/` | 主题本体 | ❌ 别改，改了升级主题会冲突 |
| `public/`、`resources/` | 构建产物，自动生成 | ❌ 已在 .gitignore |

---

## 三、写文章

### 3.1 两种写法

**带图片 / 封面（推荐统一用这种，叫"页面包"）**

```
content/posts/my-new-post/
├── index.md      ← front matter + 正文
├── cover.jpg     ← 封面图（可选）
└── screenshot.png ← 正文里的图
```

文章 URL 会是 `/posts/my-new-post/`。

**不带图片（单文件）**

```
content/posts/my-new-post.md
```

### 3.2 文件命名规则

- 用**英文小写 + 连字符**：`stm32-uart-notes`
- 别用中文、空格、大写。它们会变成 URL 里的一串 `%E4%B8%AD%E6%96%87`，又长又丑
- 文件名就是 URL，改文件名 = 改文章地址，已发过的文章别随便改

### 3.3 front matter 字段

写在文件最开头，用 `---` 包起来：

```yaml
---
title: "文章标题"                     # 必填
date: 2026-09-11                     # 必填，发布日期，格式 YYYY-MM-DD
draft: false                         # 必填，true = 只在本地预览，线上不发布
description: "一句话说明，给搜索引擎和分享卡片用"
categories: ["技术笔记"]              # 分类，通常 1 个
tags: ["Hugo", "教程"]               # 标签，3~6 个
series: ["嵌入式入门"]                # 可选，连续文章才用
featureimage: "cover.jpg"            # 可选，封面图 / 列表缩略图
lastmod: 2026-09-12                  # 可选，最后修改日期
weight: 10                           # 可选，数字越小越靠前
showToc: false                       # 可选，这篇不显示目录
comments: false                      # 可选，这篇不开评论
---
```

字段说明：

| 字段 | 作用 | 不写会怎样 |
| --- | --- | --- |
| `title` | 文章标题 | 页面没标题 |
| `date` | 发布日期，影响排序和归档分组 | 用文件修改时间 |
| `draft` | `true` 时线上不发布 | 默认 `true`（**容易忘，发布前检查**） |
| `description` | **只**进 `<meta description>` 和分享卡片，不进列表摘要 | 分享出去没描述 |
| `categories` | 分类，见第五节 | 不进分类页 |
| `tags` | 标签，见第五节 | 不进标签页 |
| `series` | 系列 | 无 |
| `featureimage` | 封面 / 列表缩略图 | 列表条目没有缩略图 |

### 3.4 列表页的摘要到底从哪来（实测结论）

博客列表里那段小字，取的是 Hugo 的 `.Summary`，规则按优先级：

1. **正文里有 `<!--more-->`** → 用分隔符**之前**的内容（此时 front matter 的 `summary:` 会被忽略）
2. 没有 `<!--more-->`，但写了 `summary: "..."` → 用 front matter 的 summary
3. 两个都没有 → **列表里没有摘要**（本站 `summaryLength = 0`，自动截断是关闭的）

👉 **所以：想让列表有条目摘要，正文里必须写一行 `<!--more-->`。**
建议放在开头第一段之后：

```markdown
开头这一段会出现在列表页。

<!--more-->

正文从这里开始，列表页不显示。
```

### 3.5 正文写法

- 标题从 `##` 开始（`#` 已经被文章标题占用了）
- 右侧目录自动生成，收录 `##` 到 `####`（在 `config/_default/markup.toml` 里配的）
- 支持加粗、斜体、删除线、行内代码、链接、表格、引用、任务列表
- 支持 emoji（`:smile:` 这种直接写）
- 数学公式：行内 `\( ... \)`、块级 `$$ ... $$`（Markdown 不会把它转义坏），另有 `{{< katex >}}` shortcode

---

## 四、图片放哪（三种放法，全都实测过）

| 放法 | 放哪 | 正文怎么写 | 结果 |
| --- | --- | --- | --- |
| **页面包**（推荐） | `content/posts/我的文章/图.jpg` | `![说明](图.jpg)` | Hugo 自动压缩、生成自适应尺寸、点击放大 |
| `assets/` | `assets/img/图.jpg` | `![说明](img/图.jpg)` | 同样处理，适合全站共用的图 |
| `static/` | `static/img/图.jpg` | `![说明](/img/图.jpg)` | **原样输出，不压缩**，路径必须带开头的 `/` |

实测渲染结果（本站真实构建输出）：

```text
页面包：![图](inline.jpg)        →  <img src="/posts/xxx/inline_hu_9282e0....jpg">   自动缩到 800px
assets：![图](img/photo.jpg)     →  <img src="/img/photo_hu_9282e0....jpg">           自动缩到 800px
static：![图](/img/static.jpg)   →  <img src="/img/static.jpg">                       原图，无处理
```

**建议**：文章配图一律用页面包，图片和 `index.md` 放一起，引用只写文件名。备份、迁移、删文章都是整包一起走，不会出现"图还在但文章删了"的孤儿图。

### 4.1 封面图 / 列表缩略图

两种方式，任选其一：

1. front matter 里写 `featureimage: "cover.jpg"`
2. **什么都不写**，只要页面包里有一张文件名含 `cover` / `feature` / `thumbnail` 的图，主题自动认出来

注意：

- 建议横图，1500×1000 左右（列表缩略图按 1.5:1 裁切，方图会被切掉上下）
- 单张控制在 500KB 以内，图太大会拖慢构建
- 不想要封面就写 `hideFeatureImage: true`

### 4.2 全站共用的图

| 图片 | 位置 | 配置在哪 |
| --- | --- | --- |
| 头像 | `assets/img/photo.jpg` | `config/_default/params.toml` 的 `image = "img/photo.jpg"` |
| 页面背景 | `assets/images/site-background.svg` | `params.toml` 的 `defaultBackgroundImage` |
| 网站图标 | `static/favicon*.png` 等 | 浏览器自动读取 |

---

## 五、分类、标签、系列

### 5.1 三者的区别

| | 用途 | 一篇写几个 | 例子 |
| --- | --- | --- | --- |
| **categories** 分类 | 粗粒度归类，"这是哪一类的文章" | 1 个（最多 2 个） | 技术笔记、随笔、读书笔记 |
| **tags** 标签 | 细粒度关键词，"讲到了什么" | 3~6 个 | Hugo、STM32、部署 |
| **series** 系列 | 多篇连载，会生成"系列"导航 | 0 或 1 个 | 嵌入式入门 |

### 5.2 本站现有的分类和标签

分类：`技术笔记`、`随笔`、`读书笔记`
标签：`Cloudflare`、`部署`、`Hugo`、`开始`、`博客`、`教程`、`Markdown`、`排版`、`读书`、`笔记法`

### 5.3 怎么写

直接在 front matter 里写数组，中文直接写，不用引号也行（有特殊字符再加）：

```yaml
categories: ["技术笔记"]
tags: ["Hugo", "部署"]
```

**不用手动去 `content/categories/` 里建文件。** Hugo 会根据 front matter 自动生成 `/categories/技术笔记/`、`/tags/Hugo/` 页面，导航栏的"分类""标签"会自动列出来。

（`content/categories/_index.md` 这类文件只决定栏目页的标题，现在写的是"分类"/"标签"/"系列"，不用动。）

### 5.4 命名建议

- 分类**宁少勿多**：控制在 3~5 个，多了就等于没分类
- 标签宁多勿杂：一篇 3~6 个，同一件事用同一个词
- 大小写和写法要统一：写了 `Hugo` 就别再写 `hugo`、`HUGO`（会算成三个标签）
- 别用 `#`、`/` 这类符号，会进 URL
- 从没写过的分类/标签，第一次写就自动建好了

### 5.5 系列

```yaml
series: ["嵌入式入门"]
```

同一系列的文章，文章页底部会自动出现系列导航（默认折叠，点开看全部）。

---

## 六、本地预览

### 6.1 现状

本机**没有装 Hugo**（`hugo` 不在 PATH 里），`git` 是有的。

### 6.2 两个选择

**A. 装一个便携版 Hugo（推荐）**
装好后本地就能实时预览，改一个字浏览器自动刷新。需要的话我帮你装到固定目录并加进 PATH。

**B. 不装，直接推 GitHub**
推上去让 Cloudflare 构建，1 分钟后看线上。缺点是打错字要等一轮，还得多推几次。

### 6.3 装好之后的常用命令

```bash
hugo server -D              # 启动本地预览，draft: true 的文章也能看到
hugo server -D -p 1314      # 1313 端口被占用时换个端口
hugo --gc --minify          # 正式构建自检，不报 ERROR/WARNING 就没问题
```

打开 http://127.0.0.1:1313/ 。`Ctrl+C` 停止。

---

## 七、发布上线

```bash
git add -A
git commit -m "新增：STM32 串口笔记"
git push
```

推送到 `main` → Cloudflare Pages 自动构建（约 1 分钟）→ 刷新 https://euinblog.pages.dev/ 。

注意事项：

- `draft: true` 的文章**不会**被构建上线
- 推送前最好本地 `hugo server -D` 看一眼，别直接推错别字
- 推分支（不是 main）会生成一个分支预览域名，可以拿来给朋友看草稿
- 部署完如果还是旧页面，`Ctrl+F5` 强制刷新试试（CDN 缓存）

---

## 八、常见坑

1. **文件必须是 UTF-8 无 BOM**。Windows 记事本"另存为 UTF-8"有时会带 BOM，会导致 front matter 解析失败、标题变成乱码。建议用 VS Code / Typora / Obsidian。
2. front matter 的 `---` **必须在文件第一行**，前面不能有空行或空格。
3. 日期写 `2026-09-11`，不要写 `2026/9/11` 或 `9月11日`。
4. `draft: true` 忘了改成 `false`，结果线上找不到文章——发布前养成检查习惯。
5. 图片路径**大小写敏感**（构建机是 Linux）：文件名 `Cover.jpg`，写成 `cover.jpg` 就会 404。
6. 正文里的 `<!--more-->` 别忘了，否则列表页没摘要。
7. 别改 `themes/blowfish/` 里的东西；要覆盖主题模板就放到根目录 `layouts/` 下同名文件。
8. `config/_default/markup.toml` 是 Blowfish 依赖的，别动。
9. `public/`、`resources/` 是构建产物，不用管，也别提交（已在 .gitignore）。

---

## 九、想改站点本身，改哪

| 想改什么 | 改哪个文件 |
| --- | --- |
| 站名、作者、邮箱、头像、背景、颜色 | `config/_default/params.toml`、`config/_default/languages.zh.toml` |
| 顶部导航菜单、页脚菜单 | `config/_default/menus.zh.toml` |
| 首页那段欢迎文案 | `content/_index.md` |
| 关于页 | `content/about.md` |
| 归档页结构 | `layouts/_default/archives.html` |
| 中文字体、正文宽度、自定义样式 | `assets/css/custom.css` |
| 要不要显示日期 / 字数 / 阅读时长 / 点赞 | `params.toml` 的 `[article]`、`[list]` 段 |

---

## 十、最常用的一份完整模板

复制这个改成自己的就行：

````markdown
---
title: "STM32 串口调试踩坑记录"
date: 2026-09-11
draft: false
description: "记录这次串口通信遇到的问题和解决办法。"
categories: ["技术笔记"]
tags: ["STM32", "串口", "调试"]
featureimage: "cover.jpg"
---

一句话说清这篇文章解决什么问题。

<!--more-->

## 问题现象

描述现象……

## 原因分析

分析过程……

## 解决办法

```c
// 代码示例
```

## 小结

- 要点一
- 要点二
````
